model ContinueToken {
    id        String            @id @default(uuid())
    token     String            @unique
    note      String?
    type      ContinueTokenType @default(ANDROID)
    createdAt DateTime          @default(now())

    @@map("continue_tokens")
}
